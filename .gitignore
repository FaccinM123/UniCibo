node_modules
dist
.firebase
firebase-debug.log
.env
.env.local
.env.save
.DS_Store
scripts/serviceAccountKey.json

# materiale personale di riferimento, non parte del progetto
*.mov

# stato di sessione locale degli strumenti, non del progetto
.claude/settings.local.json
.claude/scheduled_tasks.lock

# APK generato in locale per la pagina di download diretto (public/scarica.html):
# troppo grande e specifico della macchina per stare nel repo, si rigenera con
# npm run build:android + assembleDebug (vedi README).
public/downloads/

# Chiave di firma release Android e sue password: MAI nel repo. Se persa,
# non sarà più possibile pubblicare aggiornamenti della stessa app su Play
# Store — conservare entrambe (file + password) in un posto sicuro e separato
# (es. password manager), non solo su questa macchina.
android/*.keystore
android/keystore.properties
